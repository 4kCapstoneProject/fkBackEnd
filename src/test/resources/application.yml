spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/fkBackEnd
    username: oldaim
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

